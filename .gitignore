# Dependencies
node_modules/
**/node_modules/

# Build output
dist/
**/dist/
*.tsbuildinfo
**/*.tsbuildinfo

# Environment & secrets — NEVER commit (only .env.example is tracked)
.env
**/.env
daemon.env
secrets.enc

# Runtime data directory (default BOOKSOCIAL_DATA_DIR = <project-root>/data):
# DB + media + generated + books + music + sdcpp. NEVER commit.
/data/

# Local database (SQLite) & local data
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Python (NLP pre-pass virtualenv — ricreato in locale con server/nlp/setup.sh)
.venv/
**/.venv/
__pycache__/
*.pyc
*.pyo

# Logs
*.log
logs/

# Editor / OS
.idea/
.vscode/
*.iml
.DS_Store

# Local tooling state (not part of the project)
.omc/
.serena/
.claude/
.remember/

# Local deploy scripts (not part of the app)
deploy/

# Local repo-review notes (not part of the app)
REPO_PUBLIC_REVIEW.md
