# Avvio rapido: `docker compose up -d --build`  →  http://localhost:8771
# I dati (DB SQLite + media + musica + libri) vivono nel volume ./data sull'host.

services:
  app:
    build: .
    image: booksocial-studio
    ports:
      - "8771:8770"
    volumes:
      - ./data:/data
    environment:
      - BOOKSOCIAL_DATA_DIR=/data
      - PORT=8770
      - HOST=0.0.0.0
      # Motore AI per la generazione testi/canone (vedi server/.env.example):
      # Default 'none': l'utente sceglie il provider dalla UI. NB: i provider testo sono CLI locali
      # (opencode/codex/claude/agy) e NON sono presenti dentro il container → vanno usati con un
      # deployment che li mette a disposizione, oppure usa Ollama.
      - CONTENT_PROVIDER=${CONTENT_PROVIDER:-none}
    # Carica eventuali altre variabili da server/.env se presente.
    env_file:
      - path: ./server/.env
        required: false
    restart: unless-stopped
